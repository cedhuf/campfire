{
  "name": "campfire",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"bun --hot server/index.ts\" \"vite\"",
    "dev:server": "bun --hot server/index.ts",
    "dev:web": "vite",
    "build": "vite build",
    "start": "bun server/index.ts",
    "preview": "bun server/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
